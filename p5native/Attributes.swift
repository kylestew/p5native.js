import UIKit

extension Canvas {
    
    // ellipseMode
    func ellipseMode(mode: RectMode) {
//        rectMode = mode
    }
    
    /*
    func rectMode(mode: RectMode) {
        rectMode = mode
    }
    // strokeCap
    // strokeJoin
    func strokeWeight(weight: Double) {
        _strokeWeight = weight
        CGContextSetLineWidth(drawCTX, CGFloat(_strokeWeight))
    }
*/
    
}
